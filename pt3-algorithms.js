// 2. Euclid
// Now use your `merge` function to complete the euclid function defined below.
// Euclid takes two arrays of equal length containing numbers, and return the
// euclidean distance between them.
// Here's a refresher on Euclidean distance:
// http://bit.ly/1kXhvck
// A sample:

var euclid = function(coords1, coords2) {
  // your code here
  // use `merge` from above
};

var x = euclid([1.2, 3.67], [2.0, 4,4])

// x should equal approximately 1.08



